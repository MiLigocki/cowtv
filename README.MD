A scraper using a specific website that's serving daily schedule for a number of Polish TV channels. 
Right now user can add more of them simply by taking a look at the StationLoader class and manually modifying the allStation list.

Basic TUI.

Requirements:
Java installed.